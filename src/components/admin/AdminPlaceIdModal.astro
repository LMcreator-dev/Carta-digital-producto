<!-- MODAL: Buscar Place ID (Google) -->
<div id="placeIdModal" class="imodal" aria-hidden="true">
  <button
    type="button"
    class="imodal-backdrop"
    id="placeIdModalBackdrop"
    aria-label="Cerrar"
  ></button>
  <div class="imodal-card" role="dialog" aria-modal="true" aria-label="Buscar Place ID">
    <div class="imodal-header">
      <h3 style="margin: 0">🔎 Buscar Place ID</h3>
      <button type="button" class="imodal-close" id="placeIdModalClose" aria-label="Cerrar">
        ✖
      </button>
    </div>

    <p class="hint" style="margin-top: 8px">
      Escribe el nombre del local y selecciónalo. Te rellenará automáticamente
      <b>Google Place ID</b>.
    </p>

    <label>Buscar</label>
    <input id="placeSearchInput" placeholder="Ej: Jabali Gastrobar, Herencia" />

    <div class="grid-2" style="margin-top: 10px">
      <div>
        <label>Nombre</label>
        <div id="placeResultName" class="muted">-</div>
      </div>
      <div>
        <label>Dirección</label>
        <div id="placeResultAddr" class="muted">-</div>
      </div>
    </div>

    <label style="margin-top: 12px">Place ID</label>
    <div class="grid-2" style="align-items: center">
      <code
        id="placeResultId"
        class="place-result-code"
        >-</code
      >
      <button type="button" id="usePlaceIdBtn">Usar este Place ID</button>
    </div>

    <small class="hint">
      Requiere una API key de Google Maps con <b>Places API</b> + <b>Maps JavaScript API</b>
      activadas (y la key restringida a tu dominio).
    </small>
  </div>
</div>
